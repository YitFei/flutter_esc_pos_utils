name: example
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_esc_pos_utils:
    path: ../
  # blue_thermal_printer:
  #   git:
  #     url: https://github.com/YitFei/blue_thermal_printer.git
  #     ref: master
  blue_thermal_printer: ^1.2.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/custom_capabilities.json
